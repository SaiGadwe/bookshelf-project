# 📚 BookShelf

A Django web app where logged-in users can view and manage a list of books.

> 🚧 Work in progress — building this out step by step.

## Setup (so far)

```bash
python -m venv venv
venv\Scripts\activate      # Windows
pip install django
python manage.py migrate
python manage.py runserver
```


